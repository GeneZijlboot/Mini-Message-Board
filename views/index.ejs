<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <body>  
        <h1>Home page</h1>
        <div>
            <!-- Loop through the messages array in server.js -->
            <p>Array elements:</p>
            <div>
                <% for (let i = 0; i < messages.length; i++) { %>
                    <p><%= messages[i].text %> - <%= messages[i].user %> (<%= messages[i].added %>)</p>
                <% } %>
            </div>
        </div>
    </body>
</html>
